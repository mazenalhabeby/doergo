# Dependencies
node_modules/
.pnpm-store/

# Build outputs
dist/
build/
.next/
.expo/
out/
documentation/

# Environment files
.env
.env.local
.env.*.local
!.env.example

# IDE
.idea/
.vscode/
*.swp
*.swo
.DS_Store

# Logs
logs/
*.log
npm-debug.log*
pnpm-debug.log*

# Testing
coverage/
.nyc_output/

# Prisma
apps/api/prisma/*.db
apps/api/prisma/*.db-journal

# Mobile
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*
web-build/
ios/
android/

# Misc
*.tsbuildinfo
.turbo/

# Confidential / Internal
AI_FEATURES_ANALYSIS.md
dashboard-*.html

# Claude Code agent worktrees (accidentally committed as gitlinks)
.claude/worktrees/
worker-status-preview.png

# Local Android build artifacts (uploaded to Play via eas submit)
android-build/

# SEO working notes (not tracked)
SEO-REMAINING-LEVERS.md

# Timezone working notes (not tracked)
TIMEZONE-TODO.md

# Production secrets — NEVER track (Sec audit H15). Real values live only on the
# server in /opt/doergo/infra/docker/.env.production (untracked).
.env.production
infra/docker/.env.production

# Working scratch at repo root — screenshots, exported PDFs, and audit/planning
# notes that don't belong in the repo. Globs are root-anchored so nested assets
# aren't affected; tracked root files are re-included below.
/*.png
/*.jpg
/*.jpeg
/*.pdf
!/Doergo.pdf
/AUDIT_REMEDIATION_PLAN.md
/AUDIT_REMEDIATION_PROGRESS.md
/AUDIT_REPORT_2026-08-11.md
/hbcfield-scalable-architecture.md

# Local backup / scratch directories
docs/dashboard-spaces-backup/
emails/
apps/api/gateway/uploads/
