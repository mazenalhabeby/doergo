{
  "name": "@hbcfield/video",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Re-renderable product videos: seed a fictional org, drive the real web app with Playwright, narrate with TTS, assemble with ffmpeg.",
  "scripts": {
    "render": "node render.ts",
    "seed": "node seed-video.ts",
    "typecheck": "tsc -p tsconfig.json",
    "test": "node --test *.test.ts"
  },
  "dependencies": {
    "@hbcfield/shared": "workspace:*"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.1",
    "@prisma/client": "^6.2.0",
    "bcryptjs": "^2.4.3",
    "@types/bcryptjs": "^2.4.6",
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0"
  }
}
