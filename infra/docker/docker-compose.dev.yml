# Development compose file - only infrastructure services
# Use this for local development: docker compose -f docker-compose.dev.yml up -d

services:
  postgres:
    image: postgis/postgis:16-3.4-alpine
    container_name: hbcfield-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-hbcfield}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-hbcfield_secret}
      POSTGRES_DB: ${POSTGRES_DB:-hbcfield}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-hbcfield} -d ${POSTGRES_DB:-hbcfield}"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: hbcfield-redis
    restart: unless-stopped
    ports:
      - "${REDIS_PORT:-6379}:6379"
    volumes:
      - redis_data:/data
    command: redis-server --appendonly yes
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
    name: hbcfield-postgres-data
  redis_data:
    name: hbcfield-redis-data
