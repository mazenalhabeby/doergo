/** Shared font style-string values (CSS `font-family`), used by the showcase
 *  device components and the loading screen. NOTE: page.tsx uses Tailwind class
 *  strings (`font-[family:...]`) instead — a different thing — so it does not
 *  import these. */
export const FONT_MONO = 'var(--font-martian), monospace';
export const FONT_DISPLAY = 'var(--font-familjen), sans-serif';
