import { GenericContentSkeleton } from "@/components/skeletons/primitives"

// Its own, or the Members skeleton (a different page) flashes on the way in.
export default function Loading() {
  return <GenericContentSkeleton />
}
