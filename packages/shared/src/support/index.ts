/**
 * @hbcfield/shared — support module.
 * Ticket/message types, per-tier SLA config and priority helpers shared across
 * web, mobile and backend so support entitlements never drift.
 */
export * from './types';
export * from './sla';
export * from './routing';
