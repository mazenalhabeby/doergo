/**
 * Setup-wizard engine — shared SSOT for the guided org builder (web + mobile).
 * Pure, framework-agnostic: catalog data + a deterministic text classifier.
 */
export * from './catalog'
export * from './classify'
